primero instala los requerimientos con:

pip install -r requirements.txt

Arranca la App con:

python -m streamlit run app.py

En caso de no funcionar:

python -m pip install rdflib streamlit plotly pandas

